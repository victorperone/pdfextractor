# Runtime dependencies — structured-pdf-text
# Generated from working environment on 2026-09-14
#
# Core (always required)
pypdfium2==5.13.0
Pillow==12.3.0

# OCR (required for ExtractionMode.BALANCED / OCR and regional recovery)
# Install CPU variant — paddlepaddle-gpu for GPU inference
paddlepaddle==3.3.1
paddleocr==3.7.0
paddlex[ocr]==3.7.2

# Required by PaddleOCR internals
numpy==2.3.5
opencv-contrib-python==4.10.0.84

# Development
pytest==9.1.1

# ─── OCR model setup ───────────────────────────────────────────────────────────
#
# Model weights must be downloaded once while online. They are stored in:
#   ~/.cache/pdfextractor/paddlex/official_models/
#
# Models required (PP-OCRv5 / PP-LCNet series):
#   - PP-LCNet_x1_0_doc_ori          (document orientation classifier)
#   - PP-LCNet_x1_0_textline_ori      (text-line orientation classifier)
#   - PP-OCRv5_server_det             (text detection)
#   - latin_PP-OCRv5_mobile_rec       (text recognition — Latin script)
#
# Setup steps:
#   1. Install dependencies:
#        pip install -r requirements.txt
#   2. Download models (requires internet):
#        python -m structured_pdf_text.cli setup-models
#   3. Verify readiness:
#        python -m structured_pdf_text.cli models-status
#   4. Extract offline:
#        python -m structured_pdf_text.cli extract documento.pdf --mode balanced
#
# If the environment uses a corporate proxy with custom SSL certificates:
#   cat /etc/ssl/certs/ca-certificates.crt >> \
#       .venv/lib/python3.12/site-packages/certifi/cacert.pem
